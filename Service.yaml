apiVersion: v1
kind: Service
metadata:
  name: node-service-stable
  namespace: argocd
spec:
  selector:
    app: node
  ports:
    - port: 80
      targetPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: node-service-canary
  namespace: argocd
spec:
  selector:
    app: node
  ports:
    - port: 80
      targetPort: 3000
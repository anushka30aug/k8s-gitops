apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: node-app
  namespace: argocd
spec:
  replicas: 2

  selector:
    matchLabels:
      app: node

  template:
    metadata:
      labels:
        app: node
    spec:
      containers:
      - name: node-container
        image: anushkashukla003/node-app:76ed89697f851a8a4edf91408aaa83bc6474c23c
        ports:
        - containerPort: 3000

        readinessProbe:
          httpGet:
            path: /health
            port: 3000

  strategy:
    canary:
      canaryService: node-service-canary
      stableService: node-service-stable

      analysis:
        templates:
        - templateName: node-health-check

      steps:
        - setWeight: 20
        - pause: { duration: 30 }

        - setWeight: 50
        - pause: { duration: 30 }

        - setWeight: 100